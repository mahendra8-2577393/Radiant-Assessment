import React from 'react'

const Same = ({text,size}) => {
  return (
    <div>
        <span className="font-normal text-gray-500 leading-5 text-[13px]">{text}</span>
    </div>
  )
}

export default Same